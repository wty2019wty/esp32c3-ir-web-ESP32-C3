# ESP32-C3 红外 Web 项目安全审查报告

## 🔴 高危问题（需要立即修复）

### 1. **MQTT 密码明文存储** - `main/app_mqtt.c:59`
- **问题**：MQTT broker 密码以明文存储在 NVS 中，key 为 `"mqtt_pwd"`
- **风险**：获得设备访问权限的攻击者可直接读取 broker 连接密码，完全控制红外设备
- **修复建议**：改为 AES-GCM 加密存储，与云端 Worker 保持一致
- **状态**：`pending`

### 2. **TLS 证书校验可被用户禁用** - `main/app_mqtt.c:971`
- **问题**：用户可通过 MQTT 配置设置 `tls_skip: true` 跳过证书校验
- **风险**：中间人攻击、证书伪造、连接不安全的 MQTT broker
- **修复建议**：禁用用户关闭 TLS 校验的功能，或者仅限内部网络模式
- **状态**：`pending`

### 3. **IR 任务栈溢出风险** - `main/app_ir.c:330`
- **问题**：IR 任务栈大小 `sizeof(ir_frame_t) + 4096`，当 `IR_RAW_MAX_SEGS=2048` 时约为 8KB+4KB
- **风险**：在 ESP32-C3 上栈溢出导致系统崩溃
- **修复建议**：减小 IR_RAW_MAX_SEGS 或动态分配缓冲区
- **状态**：`pending`

### 4. **线程安全问题 - web_rpc_exec 共享调用** - `main/app_mqtt.c:533`, `main/app_web_rpc.c:141-146`
- **问题**：MQTT 任务和 HTTPD 任务同时调用 `web_rpc_exec` 无同步机制
- **风险**：竞态条件导致数据损坏或系统异常
- **修复建议**：添加互斥锁保护共享状态
- **状态**：`pending`

### 5. **NVS 数据不一致风险** - `main/app_wifi.c:82-99`
- **问题**：WiFi 配置保存时部分写入失败会导致数据不一致
- **风险**：设备无法正常启动或网络配置损坏
- **修复建议**：使用事务性操作或增加重试机制
- **状态**：`pending`

## 🟡 中危问题（近期修复）

### 6. **播放队列双重释放内存泄漏** - `main/app_ir.c:428,438,487-488`
- **问题**：队列满时同时释放 `req` 和 `req->symbols`，但播放任务中只释放了 `req->symbols`
- **风险**：内存泄漏导致系统长期运行崩溃
- **状态**：`pending`

### 7. **WebSocket 连接竞态条件** - `main/app_wifi.c:186,221`
- **问题**：`s_sta_connected` 变量在多个事件处理函数中并发修改无保护
- **风险**：WiFi 状态判断错误，导致连接异常
- **状态**：`pending`

### 8. **密码强度问题** - `Web-app/worker/index.js:32-33`
- **问题**：PBKDF2 迭代次数仅 100,000（OWASP 建议 ≥600,000）
- **风险**：离线破解成本降低约 6 倍
- **修复建议**：在文档中明确标注并确保使用强密码
- **状态**：`pending`

### 9. **MQTT 密码明文下发** - `Web-app/worker/index.js:479-488`
- **问题**：GET `/api/mqtt-config` 返回解密后的明文密码
- **风险**：浏览器端任何 XSS 可获取 broker 密码，完全控制设备
- **修复建议**：考虑后端代理 MQTT 或使用短期凭证
- **状态**：`pending`

### 10. **PUT `/api/mqtt-config` 的 password 语义 bug** - `Web-app/worker/index.js:457-467`
- **问题**：`undefined` 被转成 `''`，落入"清除密码"分支，静默清空已存密码
- **风险**：API 语义与文档相悖，数据丢失陷阱
- **修复建议**：区分 `body.password === undefined` 与 `''`
- **状态**：`pending`

### 11. **MQTT 凭证断开后仍驻留内存** - `Web-app/src/mqtt.js:107-127,149-161`
- **问题**：断开后模块级 `cfg` 对象（含 `password`）未置空，明文密码一直驻留内存
- **风险**：低风险卫生问题
- **状态**：`pending`

## 🔵 低危问题（可接受风险）

### 12. **DHCP 超时处理不够健壮** - `main/app_wifi.c:311-324`
- **问题**：依赖固定超时，路由器响应慢时可能误判
- **影响**：用户体验，低安全风险
- **状态**：`pending`

### 13. **播放队列长度过短** - `main/app_ir.c:251`
- **问题**：队列仅 4 个，高频信号可能溢出
- **影响**：部分红外信号丢失
- **状态**：`pending`

### 14. **JSON 数值解析边界检查** - `main/app_web_rpc.c:154-156`
- **问题**：数值解析缺少范围验证
- **风险**：潜在的整数溢出
- **状态**：`pending`

### 15. **帧推送与历史拉取重复插入同一帧** - `Web-app/src/App.vue:190-194`, `Web-app/src/components/LearnPanel.vue:248-253`
- **问题**：推送路径无去重逻辑，可能导致重复计数
- **影响**：功能瑕疵，非安全问题
- **状态**：`pending`

### 16. **登录接口存在用户名枚举时序侧信道** - `Web-app/worker/index.js:262-266`
- **问题**：PBKDF2 执行时间差可用于确认有效用户名
- **风险**：配合默认用户名 `admin`，枚举价值有限
- **状态**：`pending`

### 17. **登录限流与 token 吊销均依赖 KV 最终一致性** - `Web-app/worker/index.js:150-154,167-193,284-293`
- **问题**：跨 PoP 最终一致（最长约 60 秒），限流可被绕过，token 有 60 秒延迟窗口
- **风险**：尽力而为的减速带，实际影响有限
- **状态**：`pending`

## ✅ 安全亮点

### 优秀的实践：
1. **认证机制完善**：
   - PBKDF2-SHA256 密码哈希 + 随机盐
   - HMAC-SHA256 token 签名 + 版本吊销机制
   - IP 限流 + 会话管理

2. **云端安全设计**：
   - AES-GCM 加密存储敏感数据
   - CORS 白名单 + 安全头设置
   - 版本化 token 吊销机制

3. **代码质量高**：
   - 无 v-html、innerHTML 等 XSS 风险
   - 输入验证充分
   - 错误处理机制完善

### 良好的权衡：
- 虽然 MQTT 密码明文下发存在风险，但有文档说明并提及解决方案
- PBKDF2 迭代次数受平台限制，有充分说明

## 📋 修复优先级计划

### 立即行动（1-2周内）
- [ ] 修复 MQTT 密码明文存储问题（高危#1）
- [ ] 禁用 TLS 证书校验关闭功能（高危#2）
- [ ] 解决 IR 任务栈溢出风险（高危#3）
- [ ] 修复线程安全问题（高危#4）
- [ ] 修复 NVS 数据不一致问题（高危#5）

### 短期计划（1个月内）
- [ ] 修复播放队列内存泄漏（中危#6）
- [ ] 改进 WebSocket 竞态条件（中危#7）
- [ ] 修复 PUT MQTT 配置 password 语义 bug（中危#10）
- [ ] 清理 MQTT 断开后内存残留（低危#11）

### 中期改进（3个月内）
- [ ] 考虑 MQTT 后端代理方案（中危#9）
- [ ] 增强密码强度配置（中危#8）
- [ ] 扩展播放队列容量（低危#13）
- [ ] 增加数值解析边界检查（低危#14）

### 长期优化（3个月以上）
- [ ] 改进 DHCP 超时处理机制（低危#12）
- [ ] 修复帧重复插入问题（低危#15）
- [ ] 增强登录接口时序防护（低危#16）
- [ ] 优化 token 吊销最终一致性（低危#17）

## 🔍 审查信息

- **审查日期**：2026年9月7日
- **审查工具**：Code Analysis, Static Security Scan, Manual Review
- **覆盖范围**：
  - 固件代码（main/）：C 语言实现，ESP32-C3 红外控制
  - Web 前端（Web-app/）：Vue 3 + Cloudflare Worker
  - 网络协议：WebSocket + MQTT 双通道
  - 数据存储：NVS + Cloudflare KV
- **总计问题数**：17个（高危5个、中危6个、低危6个）

## 📝 备注

1. **整体安全评价**：这是一个整体安全素养很高的嵌入式 Web 项目，主要问题集中在 MQTT 模块、多线程同步和内存管理方面。没有发现可直接利用的高危漏洞。

2. **架构性考量**：部分中危问题（如 MQTT 密码明文下发）属于架构性权衡，已在文档中说明，需要在部署时注意安全配置。

3. **建议监控**：修复高危问题后，建议进行压力测试和安全性验证，确保修复效果。

4. **持续改进**：建议建立定期安全审查机制，特别是在添加新功能时进行安全影响评估。